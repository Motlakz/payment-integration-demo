/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['cdn.simpleicons.org', 'localhost', 'payment-integration-demo.vercel.app'],
  },
};

export default nextConfig;
